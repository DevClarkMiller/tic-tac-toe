// import { Game, Coord, CellState } from "../helpers/GameHelper";
// import { minmax } from "../helpers/GameTheoryHelper";

// self.onmessage = (event: MessageEvent<any>) => {
//   const { gameState, depth, maximizingPlayer, maximizingSym } = event.data;
//   const
//   const result = minmax(game, depth, maximizingPlayer, maximizingSym);
//   self.postMessage(result);
// };
